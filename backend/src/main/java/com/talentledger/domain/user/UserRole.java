package com.talentledger.domain.user;

/**
 * The role assigned to a user within TalentLedger.
 *
 * <p>Determines feature access and permissions.
 * Pure Java enum — zero framework annotations.
 */
public enum UserRole {
    USER,
    PREMIUM,
    ADMIN
}
